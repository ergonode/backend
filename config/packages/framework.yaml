framework:
    secret: '%env(APP_SECRET)%'
    csrf_protection: false
    default_locale: 'en'
    translator:
        fallback: ['en']
    php_errors:
        log: true
    cache:
        pools:
            aggregate.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 7200