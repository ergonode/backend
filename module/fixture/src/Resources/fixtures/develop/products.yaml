Ergonode\Product\Domain\Command\CreateProductCommand:
    product_{1..10000}:
        __construct:
            - '<productId(SKU_<current()>)>'
            - '<sku(SKU_<current()>)>'
            - []
            -
                'esa_template': '<stringValue(@template->id)>'
                'image_attribute': '<stringValue(@image_*->id)>'
